#define macro CONSTRUCTOR() = takes(0) returns(0) {
    __RETURN_RUNTIME(push0)
}

#define macro MAIN() = takes(0) returns(0) {
    0x20 calldataload
    0x00 calldataload

    add

         0x0 mstore
    0x20 0x0 return
}
